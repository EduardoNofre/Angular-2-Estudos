import { DirNgElseDirective } from './dir-ng-else.directive';

describe('DirNgElseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirNgElseDirective();
    expect(directive).toBeTruthy();
  });
});
