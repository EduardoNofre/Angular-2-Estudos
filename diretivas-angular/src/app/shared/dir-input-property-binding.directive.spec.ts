import { DirInputPropertyBindingDirective } from './dir-input-property-binding.directive';

describe('DirInputPropertyBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new DirInputPropertyBindingDirective();
    expect(directive).toBeTruthy();
  });
});
