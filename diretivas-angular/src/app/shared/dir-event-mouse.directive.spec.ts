import { DirEventMouseDirective } from './dir-event-mouse.directive';

describe('DirEventMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirEventMouseDirective();
    expect(directive).toBeTruthy();
  });
});
