import { DirFundoAmareloDirective } from './dir-fundo-amarelo.directive';

describe('DirFundoAmareloDirective', () => {
  it('should create an instance', () => {
    const directive = new DirFundoAmareloDirective();
    expect(directive).toBeTruthy();
  });
});
