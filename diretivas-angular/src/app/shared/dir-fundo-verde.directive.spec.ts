import { DirFundoVerdeDirective } from './dir-fundo-verde.directive';

describe('DirFundoVerdeDirective', () => {
  it('should create an instance', () => {
    const directive = new DirFundoVerdeDirective();
    expect(directive).toBeTruthy();
  });
});
